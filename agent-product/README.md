# Bundled MSE core source

MIT TypeScript core exported from the unified MSE 0.7.0 commit recorded in
`../SOURCE_PROVENANCE.json`. The Harness build bundles this source. This directory
is not a second installable product and contains no user state or host adapters.
Keep runtime changes in the unified source and refresh this versioned export;
do not independently evolve a second copy of the rule engine here.
